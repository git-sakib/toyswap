{% extends 'base.html.twig' %}

{% block body %}
    <h1>Toy creation</h1>

    {{ form_start(form) }}
        <div class="form-group">
            {{ form_widget(form) }}
        </div>
        <input class="btn btn-success" type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('toy_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
